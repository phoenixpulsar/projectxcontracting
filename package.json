{
  "name": "projectx-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R public/. dist/",
    "start": "node server.js",
    "dev": "npm run build && node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
